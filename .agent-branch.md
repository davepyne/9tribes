# Agent: work from this worktree

This is a **separate git worktree** at `/home/frank/repos/9tribes-zone-effects` on branch `zone-effects`.

When working on zone effects, always `cd /home/frank/repos/9tribes-zone-effects` and commit here.

The main repo at `/home/frank/repos/9tribes` is on branch `main` and is NOT the right place for zone-effects work.
